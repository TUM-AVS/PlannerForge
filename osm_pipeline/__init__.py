"""OSM → SUMO → CommonRoad scenario-generation pipeline.

Keep this package self-contained (no imports from sibling PlannerForge modules)
so it can run inside the CommonRoad/SUMO conda env via osm_pipeline.run as a
subprocess.
"""
